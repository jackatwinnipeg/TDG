# 2026-09-12 部署更新

数据库与六个后台函数已部署，具体版本、前端发布流程和剩余限制见 DEPLOYMENT_STATUS.md。下方保留的是原候选包的逐项说明，不代表现在仍未部署。

# TDG Route 修复状态（2026-09-09）

本包是待部署修复候选，生产数据库、Auth 和 Edge Functions 未修改。45 项均有处理或明确边界，不能等同于 45 项均已在生产关闭。

| 编号 | 原问题 | 本次处理及边界 |
|---|---|---|
| S01 | 普通用户可自行提升为管理员 | SQL 撤销资料敏感字段直写，并收紧 RLS。 |
| S02 | 管理员修改密码时，明文新密码会进入函数日志 | 管理函数移除密码和请求正文日志；历史日志和密码轮换需另行处理。 |
| S03 | 三个打印页面存在存储型 XSS 注入入口 | 打印输出转义；浏览器验证注入内容不执行。 |
| S04 | 停用账号未在数据库和全部管理接口中真正阻断 | 数据库及函数检查启用状态。 |
| S05 | viewer 角色实际上仍有写入能力 | viewer 写入限制在数据库和前端执行。 |
| S06 | 所有已登录用户可读全体配送记录和日报 | 提供可选私有读取 SQL；全车队、路线或个人可见范围待你决定。 |
| S07 | 司机目录权限同时暴露完整资料；Fill Log 配置可被任意登录用户修改 | 最少字段目录 RPC；共享 Fill Log 配置限制管理员修改、唯一账号。 |
| S08 | 登录前查询接口可枚举员工资料 | 登录解析移入服务端并限流；撤销匿名资料查询。 |
| S09 | “下次登录强制改密码”只是标志，没有执行流程 | 强制改密页面及服务端业务限制。 |
| S10 | 历史记录可直接改写或永久删除，缺少业务审计保护 | 数据库记录身份保护、不可由客户端改写的审计；封账期限及修订原因流程仍待业务设计。 |
| D01 | 登录同步丢失 Reload/Adjustment，随后可能把缺失写回云端 | 保留补液、调整及原始字段映射。 |
| D02 | 登录页“今天”使用 UTC，温尼伯傍晚会拉错日期 | 工作日期按本地日历计算。 |
| D03 | Off Duty 忽略逐条同步失败，仍清除本地记录 | 上传和最终日报确认成功后才清理本地状态。 |
| D04 | 超时重试会生成新 ID，可能重复记账 | 持久化客户端记录 ID；重复响应验证已有记录。 |
| D05 | 浏览器缓存没有账号隔离，切换账号可能串单或暴露数据 | 验证身份后绑定账号独立缓存；不自动导入旧的无归属缓存。 |
| D06 | “每天只拉一次”标记与实际数据生命周期不一致 | 移除跳过余额刷新行为，并要求已加载可信期初值。 |
| D07 | 无版本检查的全量回写，会覆盖其他设备的修改或复活删除记录 | 取消无条件覆盖；编辑删除按稳定 ID 和更新时间并发检查。 |
| D08 | 待同步队列没有消费流程，反而在读取时被删除 | 持久化失败队列并重放；不再调用猜测的备份 API。 |
| D09 | 表单工作日、历史草稿与实际查询日期不一致 | 按选定工作日期保存；旧草稿不覆盖新日期班次。 |
| D10 | 非指定车辆遇到休息日或零起始量时，余量结转失效 | 余额跨空白日期累计，保留有效零值。 |
| D11 | 车辆余量被限制为当前司机的数据，交接车辆会算错 | 保留现有司机加车辆账本；是否改为全车队车辆账本待你决定。 |
| D12 | 余量跨日截断导致累计损耗；不同打印路径取整也不同 | 内部保留精确公斤值，仅显示时截断。 |
| D13 | 配送与里程缺少关键校验，数据库也未兜底 | 数量、车辆、里程及期初值验证；数据库增加约束。 |
| D14 | 历史更正不会传播到后续日期起始余额 | 打印重新计算期初累计值；历史更正的业务封账规则待决定。 |
| D15 | 车辆/日期异步切换可能应用过时响应 | 异步返回检查账号、日期及操作上下文。 |
| D16 | 弱网下缺少可靠的操作保存，存储失败也可能被吞掉 | 存储失败显式报错、弱网队列保护；首次离线登录和完整离线 PWA 未实现。 |
| P01 | 单日 TDG 打印未等待认证初始化，可能打开空白 | 打印等待验证登录。 |
| P02 | 单日/批量 TDG 表把不同车辆的记录混算 | 批量日报按司机、日期、车辆分组。 |
| P03 | 历史打印无法正确恢复下班时间和最终里程 | 从匹配车辆班次、最终日报及记录回退读取 off duty 和里程；不会编造缺失历史时间。 |
| P04 | 详情查询失败会显示旧的、可能不属于当前 ID 的缓存记录 | 远程空结果或错误不回退到无关旧缓存。 |
| P05 | Fill Log 的 Saved 只保存单个浏览器缓存，无法成为客户资料 | 单份及批量 Fill Log 使用共享设备配置。 |
| P06 | 单张历史详情/Fill Log 未区分补液与调整 | 事件与配送分别呈现和统计。 |
| P07 | 查询未做服务端分页，历史与累计余额可能被静默截断 | 分页持续读取，兼容服务器较低行数上限。 |
| P08 | 编辑仅更新顶层字段，raw 和已存日报快照保持旧值 | 编辑同步 raw 中对应字段；最终日报头优先使用最终数据。 |
| A01 | 删除用户会级联删除其全部历史业务数据 | 停用代替删除账号，历史外键禁止级联删除。 |
| A02 | 可把最后一个管理员降级或停用，造成管理入口锁死 | 数据库序列化计数保护最后一个启用管理员。 |
| A03 | 创建用户时选择 disabled 仍会创建为 active | 统一处理 isActive / is_active。 |
| A04 | 修改用户分两步提交，失败可导致 Auth 与资料不一致 | 账号操作互斥及版本检查，Auth/资料部分失败明确报错；不能承诺跨服务原子事务。 |
| A05 | CSV 解析器会破坏含逗号的公司名称和地址 | CSV 支持引号、逗号、换行和转义引号。 |
| A06 | CSV 导入会把未提供的地址/城市/路线覆盖为空 | 必需列、无效行、重复编号检查及覆盖确认。 |
| M01 | 压缩包不是可完整重建线上系统的源码，旧安全报告与当前代码不符 | 退役旧入口，提供静态发布白名单生成器。 |
| M02 | 依赖版本浮动，页面和函数加载的 SDK 可能不一致 | 固定 Supabase SDK 版本；并不代表依赖永远无漏洞。 |
| M03 | 安全顾问仍有配置加固项，需按实际用途处理 | 提供函数 search_path 加固；泄露密码保护等 Auth 控制台设置待部署检查。 |
| M04 | 周循环在夏令时切换后少算一周 | 按 UTC 日历日差计算周次，避免夏令时偏移。 |
| M05 | 演示数据、重置规则说明与界面细节易造成误导 | 移除演示司机及自动示例客户；保留实际重置常量 2026-08-09，日期需你确认。 |

## 仍需决定或部署验证

- S06：记录共享范围。D11：车辆余额是否跨司机累计。
- 现有班次表按司机/日期唯一，同一天多车辆的完整独立班次历史需要结构调整。本包避免把另一辆车的时间混入日报，但不会创造缺失记录。
- 重置日期、历史封账和修订规则需确认。旧数据不会被自动修正；NOT VALID 约束不会清洗存量异常。
- SQL、Edge Functions、Auth 配置和前端须在隔离环境一起验证后部署。现有权限风险在生产部署前仍存在。
- 账号操作锁无自动过期；进程中断后需确认原操作结束并核对 Auth/资料状态，再由管理员按对应令牌恢复。
- 登录限流、真实邮件、Auth 管理 API、跨设备和生产并发未做真实端到端测试。测试使用模拟数据，未向生产写入。
